# Never commit real secrets — .env.example documents variable names only,
# .env.op contains only op:// references, never real values.
**/.env
**/.env.*
!**/.env.example
!**/.env.op
**/secrets/

# macOS
.DS_Store
