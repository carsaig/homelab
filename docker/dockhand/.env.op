# 1Password references only — safe to commit, resolves to real values at
# deploy time via the deploy.yml workflow (1Password/load-secrets-action).
# Never put real values in this file.

POSTGRES_PASSWORD=op://SECRETS/Dockhand/add more/database-pw
TAILSCALE_OAUTH_CLIENT_ID=op://SECRETS/Tailscale/Dockhand/client-id-oauth
TAILSCALE_OAUTH_CLIENT_SECRET=op://SECRETS/Tailscale/Dockhand/client-secret-oauth
