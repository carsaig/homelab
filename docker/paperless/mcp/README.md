# Paperless MCP

Bearer-protected remote MCP access to a Paperless-ngx instance. The gateway and
application use separate, least-privilege service identities.

All deployment coordinates and credentials are required environment variables.
